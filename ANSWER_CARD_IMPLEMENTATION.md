# 随机练习 - 答题卡功能实现

## 功能概览

在随机练习页面实现了一个"答题卡"面板，用于实时展示答题状态和支持快速跳转。

## 核心特性

### 1. 页面布局
- **左侧**（3/4宽度）：题目内容展示区
- **右侧**（1/4宽度）：答题卡面板（固定宽度280px）

### 2. 答题卡面板功能

#### 顶部统计信息
- 已出现题目数：显示被随机抽到的题目总数
- 正确数：用绿色标签显示做对的题目数
- 错误数：用红色标签显示做错的题目数

#### 题目网格区域
- 仅显示已出现（被随机抽到）的题目
- 未出现的题目不显示
- 网格布局，每行显示多个题号

### 3. 答题状态标识

| 状态 | 样式 | 颜色 |
|------|------|------|
| **正确** | 填充背景 + 彩色边框 | 绿色(tertiary) |
| **错误** | 填充背景 + 彩色边框 | 红色(error) |
| **未作答** | 透明背景 + 浅色边框 | 灰色 |
| **当前题** | 强调背景 + 加粗边框 | 蓝色(primary) |

### 4. 交互功能
- 点击答题卡中的任何题号可快速跳转回该题目
- 当前正在做的题目用蓝色高亮显示
- 不能修改已做过的题目答案（答案提交后锁定）

## 数据结构

### 状态管理
```dart
// 已出现题目ID列表（按出现顺序）
final List<String> _seenQuestionIds = [];

// 每个题目的答题状态
final Map<String, AnswerStatus> _questionStatusMap = {};

// 答题状态枚举
enum AnswerStatus { 
  unseen,        // 未出现
  unanswered,    // 已出现但未作答
  correct,       // 正确
  incorrect      // 错误
}
```

### 生命周期
1. 页面加载时，第一个题目被标记为"已出现"且"未作答"
2. 用户回答并提交后，题目状态更新为"正确"或"错误"
3. 点击下一题时，新题目被标记为"已出现"且"未作答"
4. 点击答题卡跳转时，可以查看之前做过的题目

## 实现细节

### 关键方法

#### `_submitAnswer()`
- 验证答案并判断正确性
- 更新答题卡中该题的状态

#### `_nextQuestion()`
- 移动到下一题
- 自动将新题目标记为"已出现"和"未作答"

#### `_jumpToQuestion(int index)`
- 直接跳转到指定题目
- 清空已选择的答案和解释显示

#### `_buildAnswerCard()`
- 构建单个答题卡块
- 根据状态设置不同的样式

## 用户交互流程

```
1. 打开随机练习
   ↓
2. 显示第1题（答题卡显示"1"未作答状态）
   ↓
3. 选择答案并点击"提交"
   ↓
4. 系统判断答案
   ├─ 正确 → 题号变绿色
   └─ 错误 → 题号变红色
   ↓
5. 点击"下一题"
   ├─ 显示第N题
   └─ 答题卡添加新题号（灰色未作答）
   ↓
6. 可以点击答题卡中的任何题号快速回到该题
   ↓
7. 重复步骤3-6直到完成或退出
```

## 视觉设计

### 答题卡块样式

**正确（绿色）**
```
┌─────┐
│  1  │  ← 绿色填充背景，绿色边框
└─────┘
```

**错误（红色）**
```
┌─────┐
│  2  │  ← 红色填充背景，红色边框
└─────┘
```

**未作答（灰色）**
```
┌─────┐
│  3  │  ← 透明背景，灰色细边框
└─────┘
```

**当前题（蓝色）**
```
╔═════╗
║  5  ║  ← 蓝色填充背景，蓝色加粗边框
╚═════╝
```

## 代码文件

**修改文件**：`lib/screens/question_drilling_screen.dart`

**行数**：约422行

**关键改动**：
1. 添加`AnswerStatus`枚举
2. 添加`_seenQuestionIds`列表和`_questionStatusMap`map
3. 重构`build()`方法添加右侧答题卡面板
4. 新增`_buildAnswerCard()`方法
5. 新增`_jumpToQuestion()`方法
6. 修改`_nextQuestion()`和`_submitAnswer()`以更新答题卡状态

## 兼容性

- 保留原有的进度保存功能
- 保留错题本记录功能
- 支持"再来一次"重新开始
- 支持返回首页

## 测试建议

1. **基础功能**
   - ✅ 点击提交，验证答题卡更新颜色
   - ✅ 点击下一题，验证新题目添加到答题卡
   - ✅ 点击答题卡的题号，验证能跳转回该题

2. **状态管理**
   - ✅ 验证正确数和错误数的统计
   - ✅ 验证已出现题目数的计数

3. **UI显示**
   - ✅ 当前题用蓝色高亮
   - ✅ 正确题目显示绿色
   - ✅ 错误题目显示红色
   - ✅ 未作答题目显示灰色

4. **进度保存**
   - ✅ 跳转后保存进度
   - ✅ 重新打开后恢复进度

## 扩展可能性

### 短期
- 添加题号搜索/筛选功能
- 添加只显示错题的选项
- 添加统计图表（饼图显示正确率）

### 中期
- 支持按难度过滤
- 添加标记星标题目功能
- 导出答题报告

### 长期
- AI分析学习弱点
- 个性化推荐重点题目
- 对标功能（与其他学生比较）

---

**实现完成日期**：2026年1月19日  
**编译状态**：✅ 通过（0个错误）  
**集成状态**：✅ 已完成
